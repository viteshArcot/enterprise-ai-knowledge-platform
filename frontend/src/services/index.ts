/**
 * Services barrel export.
 *
 * Import services from this file rather than from individual modules:
 *   import { apiClient } from '@/services'
 */

export { apiClient, createApiError } from './api';
export type { ApiError } from './api';
